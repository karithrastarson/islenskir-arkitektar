#Port selection
server.port=8484

#Data source
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/arch?useUnicode=yes;characterEncoding=utf-8
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.username=admin
spring.datasource.password=admin

spring.datasource.tomcat.connection-properties=useUnicode=true;characterEncoding=utf-8;
spring.datasource.sql-script-encoding=UTF-8

# Minio Host
spring.minio.url=https://localhost:9000
# Minio Bucket name for your application
spring.minio.bucket=assets
# Minio access key (login)
spring.minio.access-key=7RE8G031B9A4JP873CI2
# Minio secret key (password)
spring.minio.secret-key=WxMwcb7om3F3lDprknJIqWCzcUhlb+s3AP7n3Xzu

#Minio
minio.bucket.name=assets
minio.default.folder=/assets
minio.access.name=minio
minio.access.secret=minio123
minio.url=http://127.0.0.1:9000

# File storage configuration
access.key.id=<your_aws_access_key_id>
access.key.secret=<your_aws_access_key_secret>
s3.region.name=us-east-1
s3.bucket.name=my-test-s3-bucket-123456